# Label authorship

The YAML corpus in this repository was coded by one author, Dr. Pavanam Thomas.

Cohen's kappa in `aibench.agreement` is a function of two label vectors. The
tests supply known tables. That arithmetic is not a second-rater study of
`cases/*.yaml`. There is no second independent human rater in this repository,
and none will be invented.

This bound is tracked as
[issue #5](https://github.com/pavanamthomas/ai-response-evaluation-benchmarks/issues/5).
